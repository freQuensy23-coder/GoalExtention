{
  "name": "chatgpt-goal",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that adds a Codex-like /goal loop to ChatGPT.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "ci": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
